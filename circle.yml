machine:
  node:
    version:
      0.10.32
  environment:
    NODE_ENV: staging

general:
  artifacts:
    - "coverage/"
    - "reports/"

dependencies:
  post:
    - npm run-script build
    - mv ./dist ./dist_e2e
    - npm run-script e2e-setup
    - npm run-script e2e-http-server:
        background: true
    - npm run-script e2e-selenium:
        background: true

test:
  override:
    - npm run-script eslint
    - npm test
    # - npm run-script codeclimate
    - npm run-script e2e

deployment:
  production:
    branch: master
    commands:
      - NODE_ENV=production npm run-script publish
      - NODE_ENV=production npm run-script clean
  staging:
    branch: devel
    commands:
      - npm run-script publish
      - npm run-script clean