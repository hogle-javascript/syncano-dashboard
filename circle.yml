machine:
  node:
    version:
      0.10.32

general:
  artifacts:
    - "coverage/"

deployment:
  staging:
    branch: devel
    commands:
      - npm install
      - NODE_ENV=staging npm run-scrip publish
      - npm run-scrip clean
