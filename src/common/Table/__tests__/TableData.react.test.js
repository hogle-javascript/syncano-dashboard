jest.dontMock('../TableData.react');

describe('TableData.react', function() {
  it('testing component', function() {

    var React = require('react/addons');
    var TestUtils = React.addons.TestUtils;
    var TableData = require('../TableData.react');
  });
});
