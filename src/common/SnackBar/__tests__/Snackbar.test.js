jest.dontMock("../Snackbar");

describe("Snackbar", function() {
  it("testing component", function() {

    var React = require('react/addons');
    var TestUtils = React.addons.TestUtils;
    var Snackbar = require('../Snackbar');
  });
});
